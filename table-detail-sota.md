# Tabel 1. Ringkasan Teknis Artikel IDS Berbasis ML/DL

> **Versi Perbaikan** — Kolom redundan digabung, kolom Baseline ditambahkan, kolom Kutipan dihapus.

| **Artikel** | **Tujuan Utama** | **Arsitektur / Model** | **Detail Arsitektur Kunci** | **Paradigma Learning** | **Dataset & Ukuran** | **Pre-processing** | **Metrik Kinerja** | **Baseline / Pembanding** | **Keterbatasan / Future Work** |
|---|---|---|---|---|---|---|---|---|---|
| **Park et al. (2025)** *Electronics* | IDS unsupervised untuk deteksi serangan baru berbasis rekonstruksi paket jaringan | CNN-BiLSTM Autoencoder | Encoder: Conv1D → BiLSTM → Dropout → Dense (latent); Decoder: RepeatVector → BiLSTM → Dropout → Conv1D → TimeDistributed | Unsupervised; binary (normal vs intrusi); training hanya pada data normal | Dataset paket jaringan (nama tidak dicantumkan); split: normal saja (train), normal+attack (test) | Gabung banyak CSV → satu dataset; batch=128, lr=0.01, Adam, MAE loss, 10 epoch | Akurasi: **98.1%** F1-score: **98.3%** | LSTM-AE; Denoising LSTM-AE | Belum klasifikasi jenis serangan; rencana eksplorasi attention, meta-learning, contrastive learning |
| **Almuhanna & Dardouri (2025)** *Frontiers in AI* | NIDS anomaly-based yang robust dan generik di dataset besar (>5,6 juta record) | Ensemble Hibrida ML+DL (Weighted Soft-Voting): XGBoost + RF + GNN + LSTM + AE | Tiap model belajar representasi berbeda (pohon, urutan, graf, sekuensial, rekonstruksi); prediksi digabung via weighted soft-voting | Supervised; multi-class (normal vs berbagai intrusi) | Dataset jaringan besar: >5,6 juta record awal; setelah cleaning: **5.093.438 instance** + 1 benchmark independen | Hapus 5.734 missing & 562.314 duplikat; StandardScaler; feature engineering (Packet_Length_Ratio dll.); SMOTE untuk imbalance | Akurasi, Presisi, Recall, F1 mendekati **~100%**; validasi 5-fold CV + dataset independen | Studi benchmark independen (tidak dirinci nama) | Detail teknis future work tidak tercantum di kutipan; dibahas di Section 6 paper |
| **Baidar et al. (2025)** *arXiv* | IDS akurat, low-latency, dan privacy-preserving untuk IoT/5G edge computing | Hybrid CNN–BiLSTM–AE (3 cabang paralel) + Federated Learning (FL) | AE branch: Dense→Dropout→bottleneck(64)→Decoder(53 dim); CNN branch: conv lokal; BiLSTM: BiLSTM(64)→BiLSTM(32)→Dense(64); Fusion: concat(192)→Dense(128,L2)→Dropout(0.4)→Dense(1,sigmoid); FL: update model dikirim ke server, data tetap di edge | Supervised biner (intrusi vs normal) dalam skema Federated Learning | UNSW-NB15 (training & testing set resmi) | Deduplikasi; winsorization outlier (p95); kontrol cardinality kategorikal; encoding & scaling konsisten; balancing kelas; *semua transformer fit di training split untuk hindari data leakage* | AUC: **99.59%** F1: **97.36%** Presisi: ~98.5% Recall: ~96.3% Latency: **~0.0476 ms/sample** (<10 ms URLLC) | Metode FL dan non-FL sebelumnya pada UNSW-NB15 | Perlu riset lanjut untuk ambient IoT dan RedCap di skala miliaran device |
| **Halbouni et al. (2022)** *IEEE Access* | Mencapai detection rate tinggi dan FAR rendah dengan hybrid CNN-LSTM untuk NIDS | Hybrid CNN-LSTM + Batch Normalization + Dropout | CNN: ekstraksi fitur spasial; LSTM: fitur temporal/sekuensial; beberapa konfigurasi diuji (jumlah layer, neuron, FC layer, dropout rate) untuk temukan konfigurasi optimal | Supervised; binary & multiclass (berbagai tipe serangan) | CIC-IDS2017; UNSW-NB15; WSN-DS | One-Hot Encoding label (benign/malicious); split 80% train / 20% test; train dibagi train/validation; Stratified K-Fold CV untuk seleksi ukuran set | Akurasi: **99.60%** FAR: **0.11%** (pada konfigurasi optimal: 3 layer, dropout=0.2, 1 FC) | Benchmarking terhadap studi terdahulu di tiga dataset | Detection rate rendah & FAR tinggi di beberapa kelas akibat class imbalance; akan ditangani di future work |
| **Singh & Jang-Jaccard (2022)** *Journal of LaTeX Templates* | Unsupervised IDS berbasis korelasi spatio-temporal yang meminimalkan kebutuhan label dan mengurangi FP/FN | MSCNN-LSTM Autoencoder (dua tahap) + Isolation Forest (dua tahap koreksi) | Tahap 1: Multiscale CNN-AE → fitur spasial → latent space; Tahap 2: LSTM-AE pada latent → fitur temporal; anomaly score dari reconstruction error; IF-1: koreksi FP; IF-2: koreksi FN | Unsupervised anomaly detection + error correction via Isolation Forest | NSL-KDD; UNSW-NB15: train 56k normal+119.341 attack, test 37k normal+45.332 attack; CICDDoS2019: benign+1% malicious (~70 juta record total) | Anomaly score dari reconstruction error; dua tahap klasifikasi Isolation Forest; *(detail preprocessing tidak dirinci penuh di paper)* | Recall & F-score **lebih tinggi dari metode unsupervised SOTA** di NSL-KDD, UNSW-NB15, dan CICDDoS2019 | Metode unsupervised state-of-the-art di tiga dataset | Belum dilakukan fine-tuning hyperparameter dan optimisasi arsitektur secara mendalam |

---

**Singkatan:** AE = Autoencoder · BiLSTM = Bidirectional LSTM · CNN = Convolutional Neural Network · FAR = False Alarm Rate · FL = Federated Learning · FN = False Negative · FP = False Positive · GNN = Graph Neural Network · IF = Isolation Forest · MSCNN = Multi-Scale CNN · RF = Random Forest · SMOTE = Synthetic Minority Oversampling Technique · SOTA = State-of-the-Art · URLLC = Ultra-Reliable Low-Latency Communication

## Referensi

- Park, H., Shin, D., Park, C., Jang, J., & Shin, D. (2025). Unsupervised Machine Learning Methods for Anomaly Detection in Network Packets. *Electronics*. https://doi.org/10.3390/electronics14142779
- Almuhanna, R., & Dardouri, S. (2025). A deep learning/machine learning approach for anomaly based network intrusion detection. *Frontiers in Artificial Intelligence*. https://doi.org/10.3389/frai.2025.1625891
- Baidar, R., Maric, S., & Abbas, R. (2025). Hybrid Deep Learning-Federated Learning Powered Intrusion Detection System for IoT/5G Advanced Edge Computing Network. *arXiv*.
- Halbouni, A., Gunawan, T., Habaebi, M., Halbouni, M., Kartiwi, M., & Ahmad, R. (2022). CNN-LSTM: Hybrid Deep Neural Network for Network Intrusion Detection System. *IEEE Access*. https://doi.org/10.1109/ACCESS.2022.3206425
- Singh, A., & Jang-Jaccard, J. (2022). Autoencoder-based Unsupervised Intrusion Detection using Multi-Scale Convolutional Recurrent Networks. *Journal of LaTeX Templates*.
